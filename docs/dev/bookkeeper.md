# 记账员 - 设计思路

## 项目概述

一个提供对话式 journalize 功能的 AI Agent。用户通过自然语言描述交易，AI 自动生成复式记账格式并写入账本。

**设计理念**：
> 让大模型做打字员，让解析器做审核员，让代码保持简单

---

## 核心架构

### 分层设计

```
┌─────────────────────────────────────┐
│           UI 层 (TUI)               │  用户交互
├─────────────────────────────────────┤
│         业务逻辑层                   │  journalize()
├─────────────────────────────────────┤
│    ┌─────────────┐  ┌────────────┐  │
│    │  Prompt     │  │  LLM       │  │  核心能力
│    │  Builder   │  │  Caller    │  │
│    └─────────────┘  └────────────┘  │
├─────────────────────────────────────┤
│    ┌─────────────┐  ┌────────────┐  │
│    │  Extractor │  │  Validator │  │  解析与验证
│    └─────────────┘  └────────────┘  │
├─────────────────────────────────────┤
│           存储层                     │  本地/云端账本
└─────────────────────────────────────┘
```

### 模块职责

| 模块 | 职责 | 边界 |
|------|------|------|
| **Prompt Builder** | 构建系统提示词 + 用户提示词 | 输入：账户列表 + 用户描述；输出：prompt |
| **LLM Caller** | 调用 LLM API | 输入：prompt；输出：文本响应 |
| **Extractor** | 从 LLM 输出提取目标格式 | 输入：文本；输出：结构化内容 |
| **Validator** | 验证语法/语义正确性 | 输入：结构化内容；输出：是否有效 |
| **Storage** | 账本读写 | 输入：路径；输出：内容 |

---

## 数据流

```
用户输入 (自然语言)
        │
        ▼
┌───────────────────┐
│  获取账户列表      │  ◄── 账本文件
└───────────────────┘
        │
        ▼
┌───────────────────┐
│  构建提示词        │  system + user prompt
│  (Prompt Builder) │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│  调用 LLM         │  LLM Caller
│  (LLM Caller)    │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│  提取目标格式     │  Extractor
│  (Extractor)     │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│  验证语法         │  Validator
│  (Validator)     │  ◄── 解析器
└───────────────────┘
        │
   ┌────┴────┐
   │ 验证失败 │  ──×── 返回错误
   └─────────┘
        │ 验证成功
        ▼
┌───────────────────┐
│  写入账本         │  Storage
└───────────────────┘
        │
        ▼
   返回结果
```

---

## 核心接口

### journalize()

```
输入：
  - user_input: string  (用户自然语言描述)
  - ledger_path: optional<path>  (账本路径)

输出：
  - success: boolean
  - message: string (结果或错误信息)

前置条件：
  1. 账本文件存在或可创建
  2. LLM 服务可用

后置条件：
  1. 成功时：账本文件追加新交易
  2. 失败时：账本文件不变
```

---

## 验证策略

### 双重验证

1. **语法验证**：使用原生解析器检查格式正确性
2. **语义验证**（可选）：
   - 借贷平衡
   - 账户存在
   - 日期格式

**原则**：验证失败时拒绝写入，保证账本一致性。

---

## 存储层抽象

### 支持的存储后端

| 后端 | 说明 | 适用场景 |
|------|------|----------|
| 本地文件 | 文件系统 | 开发/单机 |
| OSS | 阿里云对象存储 | 云端账本 |
| S3 | AWS S3 | 云端账本 |
| Git | Git 仓库 | 版本控制 |

### 账本文件格式

当前使用 **Beancount**（复式记账格式），可通过抽象层支持其他格式。

---

## 扩展建议

### 功能扩展

1. **多账本支持**
   - 添加账本选择器
   - 支持 `--ledger` 参数

2. **对话上下文**
   - 传递 chat_history 给 LLM
   - 支持多轮对话修改

3. **智能推荐**
   - 记录历史交易
   - 自动推荐常用分类

4. **批量导入**
   - 支持 CSV/Excel
   - 批量 journalize

5. **报表集成**
   - 生成收支报表
   - 可视化分析

